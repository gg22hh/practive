CREATE TABLE users(
	id SERIAL PRIMARY KEY,
	name VARCHAR(255),
	party VARCHAR(255),
	overall INTEGER,
	subject1 INTEGER,
	subject2 INTEGER,
	subject3 INTEGER,
	subject4 INTEGER,
	subject5 INTEGER,
	subject6 INTEGER,
	subject7 INTEGER,
)